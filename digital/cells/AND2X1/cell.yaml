name:        AND2X1
type:        digital
category:    row          # row | pad | frame
function:    "AND de 2 entradas"
owner:       ""                   # usuario de GitHub responsable
status:      drc_clean                  # wip | migrated | drc_clean | lvs_clean | characterized | released

process:     CIDESI_CM05
origen:      "CM05_celdas.gds del PDK CIDESI_CM05 v0.8"

geometry:
  width_um:   167.5
  height_um:  222.5
  origin:     [0.0, 0.0]
  sites:      67.0                  # ancho / pitch de 2.5
  shapes:     105

# Direcciones inferidas del nombre del pin. REVISAR Y CONFIRMAR.
pins:
  - {name: GND, direction: ground, layer: "49/1", pos: [80.0, 10.0]}
  - {name: VDD, direction: power, layer: "49/1", pos: [80.0, 202.5]}
  - {name: A, direction: input, layer: "51/1", pos: [25.0, 90.0]}
  - {name: B, direction: input, layer: "51/1", pos: [97.5, 82.5]}
  - {name: Out, direction: output, layer: "51/1", pos: [147.5, 85.0]}

site_contract:
  compliant:  true
  violations: []
  checked_by: "scripts/drc/check_site.py contra digital/site.yaml"

verification:
  drc: not_run
  lvs: not_run

# PENDIENTE: el netlist heredado (prueba.cir) solo declaraba VDD y GND como
# puertos; las entradas y la salida quedaron como nodos internos. Hay que
# re-extraer desde KLayout con el etiquetado de pines corregido.
files:
  ltspice:
    schematic: ltspice/AND2X1.asc
    symbol:    ltspice/AND2X1.asy
  layout:   layout/AND2X1.gds
  netlist:  layout/AND2X1.cir
  behavior: null                   # pendiente

notes: "Frontera chip fue corregida. La causa fue el box Die_outline que no tenia la posición correcta. Esquematico y símbolo corregido. Extracción de netlist"

# ---------------------------------------------------------------------------
# Bloqueos abiertos. Mientras esta lista no este vacia, la celda no puede
# pasar a status: released.
# ---------------------------------------------------------------------------
blockers: []
